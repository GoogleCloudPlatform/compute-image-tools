This directory contains terraform definitions that are used with module and e2e testing.

One project definition per subdirectory.

Usage:
 1. Download terraform: https://www.terraform.io/downloads.html
 2. Authenticate gcloud using an account that has permissions to create
    projects in your organization.
 3. Within a project definition directory:
     a. `terraform init`
     b. `terraform apply`
