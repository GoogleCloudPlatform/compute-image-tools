{
  "Name": "build-debian-sid-fai",
  "Vars": {
    "build_date": {"Value": "${DATE}", "Description": "Build datestamp used to version the image."},
    "debian_cloud_images_version": {
      "Value": "master",
      "Description": "The debian-cloud-images scripts git commit ID or branch to use."
    },
    "debian_version": {
      "Value": "sid",
      "Description": "The FAI tool debian version to be requested."
    },
    "google_cloud_repo": {
      "Value": "stable",
      "Description": "The Google Cloud Repo branch to use."
    },
    "publish_project": {
      "Value": "${PROJECT}",
      "Description": "A project to publish the resulting image to."
    }
  },
  "Steps": {
    "build": {
      "Timeout": "60m",
      "IncludeWorkflow": {
        "Path": "./debian_fai.wf.json",
        "Vars": {
          "build_date": "${build_date}",
          "debian_cloud_images_version": "${debian_cloud_images_version}",
          "debian_version": "${debian_version}",
          "builder_source_image": "projects/compute-image-tools/global/images/family/debian-9-worker",
          "google_cloud_repo": "${google_cloud_repo}"
        }
      }
    },
    "copy-gcs-object": {
      "CopyGCSObjects": [
        {
          "Source": "${OUTSPATH}/root.tar.gz",
          "Destination": "${OUTSPATH}/root.tar.gz"
        }
      ]
    },
    "create-image": {
      "CreateImages": [
        {
          "Name": "debian-sid-fai-v${build_date}",
          "RawDisk": {
            "Source": "${destination}"
          },
          "Description": "Debian, Debian, ${debian_version}, x86_64 built on ${build_date}",
          "Family": "debian-sid-fai",
          "Project": "${publish_project}",
          "NoCleanup": true,
          "ExactName": true
        }
      ]
    }
  },
  "Dependencies": {
    "copy-gcs-object": ["build"],
    "create-image": ["copy-gcs-object"]
  }
}
