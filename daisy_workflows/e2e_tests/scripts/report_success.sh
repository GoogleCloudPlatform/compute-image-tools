#!/bin/bash

# This script simply writes a unique string to the serial port to indicate that
# an instance has been successfully created and is healthy enough to output a
# message.

# Lots of other stuff comes out the serial port, so we need to test for a string
# more unique than just "SUCCESS".
echo "SUCCESS JO2Pd99h4qRK5HIpc5NP"

